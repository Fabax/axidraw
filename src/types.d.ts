export type FileProps = { id: string; layers?: LayerList; path: string; name: string };
export type LayerProps = { id: string; color: string; number: string; valid: boolean };
export type PreferenceValues = { speed: string; model: string; printFolder: string; axicliPath: boolean };
